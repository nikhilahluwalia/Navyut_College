// Tailwind CSS v4 configuration
// We enable class-based dark mode so the React ThemeContext can toggle
// the `dark` class on the <html> element.
/** @type {import('tailwindcss').Config} */
export default {
  darkMode: 'class',
};
